# src

## Structure

```console
src
│
├── index.tsx: redering entry point (depends on react-scripts)
├── actions: redux actions
│   └──
├── apis: fetch from apis and return json data.
│   └──
├── components: Atomic Design
│   └──
├── reducers: redux reducers
│   └──
```